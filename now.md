# Now

## 项目当前定位

这是一个用于抓取北航博雅课程、做筛选、并通过门户/邮件提供提醒的项目。

当前技术栈：

- 抓取：`Playwright`
- 后端：`Flask`
- 定时调度：`APScheduler`
- 数据库：`SQLite`
- 前端：服务端模板 + 原生 `JS/CSS`

## 当前目录结构

- `src/`
  - `main.py`：应用入口
  - `auth.py`：SSO / WebVPN 登录
  - `scraper.py`：课程列表抓取与详情补全
  - `scheduler.py`：定时任务与推送调度
  - `models.py`：SQLAlchemy 模型
  - `push/email_push.py`：邮件发送与模板
- `web/`
  - `app.py`：Flask 路由与 API
  - `templates/`：页面模板
  - `static/`：门户和控制台前端脚本与样式
- `deploy/`
  - `boya-agent.service`：systemd 服务示例
  - `nginx_boya.conf`：Nginx 反代示例
- `config/.env.example`：环境变量样例

## 部署现状

从仓库配置看，当前预期部署方式是：

- 应用本机运行在 `127.0.0.1:5000`
- `systemd` 服务名：`boya-agent`
- `Nginx` 反向代理到 Flask
- 对外域名：`https://buaaboya.top`

关键配置文件：

- [README.md](E:\Demo\boya-agent\README.md)
- [deploy/boya-agent.service](E:\Demo\boya-agent\deploy\boya-agent.service)
- [deploy/nginx_boya.conf](E:\Demo\boya-agent\deploy\nginx_boya.conf)
- [config/.env.example](E:\Demo\boya-agent\config\.env.example)

## 当前产品面

已存在的主要能力：

- 课程抓取与入库
- 门户查看课程
- 课程筛选
- 选课提醒注册
- 邮件验证 / 登录
- 门户订阅偏好设置
- 控制台管理与手动抓取

## 当前代码状态

除当前这次 `now.md` 存档更新外，仓库没有其他未提交改动。

当前代码里值得注意的点：

1. 抓取触发链路刚收稳一轮
- 控制台 `/api/trigger` 已改为默认后台触发，不再同步阻塞等待完整抓取
- 控制台和门户都补了“非 JSON / HTML 错误页”容错，避免再出现 `Unexpected token '<'`
- 示例 Nginx 已补 `proxy_read_timeout` 等超时配置，降低长请求被代理切断的概率

2. 调度并发保护比之前稳
- `scheduler.py` 现在同时做了“提交层去重 + 运行层复用/加锁”
- 高频点击“立即抓取”或门户刷新时，不容易再并发创建多次抓取任务

3. 门户首页最近修过两个明显前端回归
- “全部已满时看不到折叠区”是因为 `portal.js` 里注释和 `let fullSection` 粘到同一行，导致折叠区创建逻辑实际未执行；现已修回
- 空课程态的坏字符串和破损 `</div>` 已修掉，不再把 HTML 片段直接渲染到页面上

4. 但前端编码债还没有真正收完
- `portal.js` / `portal.html` 仍残留历史乱码字符串
- `web/static/app.js` 当前存在重复 helper / 覆盖式修补痕迹，行为已经稳定，但代码形态不够干净
- 这些不会每处都立刻炸，但会持续提高后续维护成本

## 近期已处理过的方向

结合当前代码和最近维护重点，可以确认这几个方向已经被持续处理：

- 门户登录与会话兜底
- 邮件验证链路与新域名切换
- 抓取并发 / 高频巡检
- 控制台“立即抓取”后台化与状态轮询
- 控制台 / 门户接口返回非 JSON 时的容错
- 门户空状态、已满折叠区和若干前端文案回归修复
- 门户刷新与提醒链路
- 用户门户首屏与移动端体验

## 仍需持续盯的风险

### 1. 编码问题

这是当前最稳定的技术债之一。

表现：

- 中文在 PowerShell / 部分文件里显示为乱码
- 少数坏字符串虽然已不影响当前主链路，但仍散落在 `portal.js`、`portal.html`、`README.md`

建议：

- 后续优先用 UTF-8 无 BOM 统一前端文件
- 每次改 `web/static/*.js` 后都跑一次语法检查
- 不要在终端乱码状态下大面积重写整文件

### 2. 前端 helper 重复与热修补痕迹

这是最近稳定性修复后留下的次级技术债。

表现：

- `web/static/app.js` 现在有重复定义的 API helper / refresh watcher 逻辑
- 这是为了快速收稳定性时留下的技术债，短期可用，长期应收敛成一套实现

建议：

- 下一轮前端收口时，把控制台请求 helper 和刷新轮询只保留一份实现
- 不要继续在这块叠覆盖式补丁

### 3. 提醒链路分叉

历史上“顶部待提醒数量”和“提醒列表内容”并不总是来自同一数据源。

建议：

- 所有提醒相关展示统一基于同一套提醒序列化结果
- 不要再维护多套 reminder count 口径

### 4. 热门课程的时效问题

热门课可能在几十秒内从“剩余 1 人”变成“已满”。

建议：

- 高频监控保留快抓取模式
- 前端继续保留或增强 `last_seen` / `last_seen_seconds_ago` 展示
- 对 `remaining <= 3` 的课，用更保守的按钮文案和状态提示

### 5. 门户 JS 体量偏大

`web/static/portal.js` 已经承担过多逻辑：

- 会话
- 高亮
- 课程渲染
- 提醒
- 通知
- 设置
- onboarding

建议：

- 后续拆模块，至少按：
  - session
  - courses
  - reminders
  - notifications
  - settings
  划分

## 建议的维护习惯

每次改动后，至少执行：

```powershell
python -m py_compile web/app.py src/scheduler.py src/scraper.py src/models.py
node --check web/static/app.js
node --check web/static/portal.js
```

如果是线上验证，再补：

```bash
sudo systemctl restart boya-agent
sudo systemctl status boya-agent --no-pager
```

如果动了反代配置，再补：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 如果现在继续维护，优先级建议

1. 先收门户 / 控制台前端编码和坏字符串
- 尤其是 `web/static/portal.js`、`web/templates/portal.html`、`web/static/app.js`
- 同时把 `app.js` 里的重复 helper 收成一份

2. 再收门户数据源一致性
- 提醒、通知、顶部统计统一口径
- 明确“全部已满”“可抢”“已过期”的展示边界

3. 最后再做体验优化
- 移动端布局
- onboarding
- 首屏层级与状态提示

## 后续待办规划

前提约束：

- 不影响现有主站抓取、门户、提醒、登录主链路
- 新功能尽量通过独立路由、独立数据表、独立前端资源接入
- 先保证“隔离、可灰度、可回滚”，再谈扩展

### 1. `buaaboya.top/QRcode` 页面

目标：

- 新增一个独立页面，用于上传和展示“常规签到课程”的签到二维码
- 用户可上传二维码图片，供其他用户扫码签到
- 后续再考虑内容校验、审核和风控；第一阶段可以先不做复杂验证
- 对多次上传、持续贡献的用户做通知与贡献激励

建议的落地原则：

- 路由和页面独立，不与现有 `/portal` 逻辑混在一起
- 数据模型独立，至少区分：
  - 上传记录
  - 上传用户标识
  - 课程名称 / 时间 / 上传时间
  - 二维码图片路径或对象存储地址
  - 状态（可见 / 下线 / 失效）
- 第一阶段先把“上传、展示、基础贡献统计”做通
- 第二阶段再考虑：
  - 失效二维码自动下线
  - 重复上传合并
  - 用户贡献榜 / 奖励
  - 举报 / 审核 / 黑名单

实现约束建议：

- 上传能力不要直接挂到现有门户主面板里，优先新页面独立入口
- 文件存储、访问权限、清理策略要单独考虑，避免把主站静态资源目录弄脏
- 奖励机制先做轻量统计和通知，不要一开始把积分体系做重

MVP 拆分建议：

第一阶段（最小可用）：

- 新增独立路由 `/QRcode`
- 新增独立页面模板、独立 `JS/CSS`
- 提供二维码图片上传
- 提供基础字段：
  - 上传人邮箱或会话用户
  - 课程名称
  - 课程时间
  - 上传时间
  - 二维码图片地址
  - 状态（显示 / 隐藏）
- 页面展示“最近可用二维码列表”
- 先不做复杂审核、解析、去重，只保留后台可手动下线能力

第二阶段（轻量贡献机制）：

- 统计每个用户的上传次数
- 对多次上传用户做站内提示或邮件感谢
- 门户或 `/QRcode` 页面展示基础贡献标识
- 可以先按固定阈值做轻量奖励：
  - 第 3 次上传
  - 第 10 次上传
  - 连续多日上传

第三阶段（再考虑风控和体验）：

- 二维码失效时间控制
- 重复二维码识别
- 举报 / 审核
- 排行榜或更完整的激励体系

建议不要在第一阶段做的事：

- 不要把它直接塞进现有门户课程主列表逻辑
- 不要一开始就上复杂积分、兑换、排行榜
- 不要把二维码识别、内容校验和审核流一次性做全

### 2. 网站性能与抓取准确性 / 时效性 / 稳定性优化

目标：

- 继续降低前端和接口的无效开销
- 提高抓取在高峰时段的响应速度和成功率
- 结合博雅系统特性，提升“已开抢 / 即将开抢 / 热门满员课”的识别准确性和刷新时效

建议的优化方向：

- 前端侧：
  - 收敛 `app.js` / `portal.js` 重复 helper
  - 减少重复请求和无效轮询
  - 明确空态 / 已满 / 过期 / 可抢四类展示边界
- 后端接口侧：
  - 继续把慢接口改成后台触发 + 状态轮询
  - 对状态接口和分类接口做轻量缓存或更小粒度查询
  - 明确错误页、超时、未登录三类响应语义
- 抓取调度侧：
  - 保留“全量抓取 + 快速抓取”双模式
  - 对临近开抢、已开抢、热门低余量课程做更高频巡检
  - 减少不必要的详情补全，优先把主列表时效性做高
- 结合博雅系统特性可继续探索：
  - 按选课开始时间分层调度
  - 对“当前仍在选但已满”的课程维持高优先级监控
  - 对短时间内频繁波动的课程记录更细的 `last_seen` 与波动轨迹
  - 尽量缩短从“名额出现”到“前端可见 / 邮件发出”的链路时延

建议的顺序：

1. 先收前端重复逻辑和乱码债，避免优化过程中继续放大维护成本
2. 再收接口与调度层的性能、容错和监控策略
3. 最后再做更贴近博雅系统行为的专项优化

MVP 拆分建议：

第一阶段（低风险稳态优化）：

- 收敛 `web/static/app.js` 重复 helper 和重复轮询逻辑
- 继续修掉门户 / 控制台已知坏字符串
- 给关键接口补统一的错误语义：
  - 未登录
  - 非 JSON 错误页
  - 真实业务失败
- 给抓取链路补基础耗时日志：
  - 登录耗时
  - 列表抓取耗时
  - 详情补全耗时
  - 数据入库耗时

第二阶段（直接提升时效性）：

- 保留 `full` + `quick` 双模式，并明确它们的触发场景
- 对“已开抢”“30 分钟内开抢”“当前满员但未过期”的课程单独提高巡检优先级
- 减少每轮都做的详情补全，把详情补全更多留给：
  - 新课
  - 状态变化课
  - 热门课
- 明确首页和提醒链路优先依赖主列表数据，不让慢详情拖住整体刷新

第三阶段（结合博雅系统特性做专项优化）：

- 以选课开始时间为核心做分层调度
- 对“仍在选课窗口内但已满员”的课程持续快抓取
- 对低余量课程记录更细粒度波动时间
- 把“发现名额 -> 前端可见 -> 提醒发出”的时延作为关键指标盯住

建议优先落地的三项：

1. 清理 `app.js` 重复 helper
2. 把抓取各阶段耗时打到日志里
3. 按“已开抢 / 即将开抢 / 满员未过期”做更细的快抓取策略

一句话说，这两个后续任务都可以做，但必须坚持“新页面与主链路隔离、性能优化优先收口后再扩展”的原则。

## 一句话总结

项目主链路现在比前一轮稳，尤其是抓取触发、前端容错和门户已满折叠区已经修回；但门户 / 控制台前端仍有明显编码债和热修补痕迹，下一阶段重点应该是“把前端字符串和 helper 收干净”，而不是继续叠功能。

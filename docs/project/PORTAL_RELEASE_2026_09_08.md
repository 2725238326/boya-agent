# 2026-09-08 门户改进与测试环境

## 变更范围

基线为 `0314a3b`，应用候选版本为 `e72589b`。本次不改变接口、数据库结构、认证规则或外部通知配置。

- 登录状态暂时加载失败时显示重试按钮，返回课程页展示错误；401 仍进入订阅页。
- 浏览器保存了已不存在的页签时回到课程页，避免所有面板隐藏。
- 已满课程改用原生按钮，支持 Enter、空格和 `aria-expanded`。
- 移除“立即通知”的承诺，改为说明课程详情和官方名额信息。
- 修复已满课程展开后在窄屏上保持 440px 最小列宽的问题，与主课程列表共用移动端单列规则。
- 课程加载失败时移除旧的已满课程区，不混合展示错误与旧卡片。
- 生产部署前增加离线浏览器回归，失败时不执行服务器部署。

## 验证

新测试先复现了失效页签导致页面隐藏的问题；扩展到已满课程展开检查后，检测到 390px 视口中的 440px 卡片溢出。修复后八组场景全部通过。

本地新建 `.venv`，使用 Python 3.12，与 CI 主版本一致：

```powershell
.venv\Scripts\python.exe scripts/verify_release.py
.venv\Scripts\python.exe -m playwright install chromium
.venv\Scripts\python.exe scripts/verify_portal_browser.py
```

结果：71 项测试通过、5 项 subtest 通过，没有跳过项；六个 JavaScript 文件语法检查及 TypeScript 检查通过。Chromium 在 390px 和 1280px 下覆盖无课、课程接口失败、登录状态接口失败、已满课程，共八组场景；检查重试、筛选、键盘操作、溢出、旧内容移除和脚本异常。所有浏览器请求由固定数据响应，不访问真实校园账号或发送邮件。

[功能分支 CI](https://github.com/2725238326/boya-agent/actions/runs/34178706727) 已通过。

## 发布状态

[生产工作流](https://github.com/2725238326/boya-agent/actions/runs/34178814440) 已成功，`e72589b` 已部署到 `49.233.248.86` 的 `buaaboya.top`。服务器工作树无变更，应用和 Nginx 为 active，公开健康检查成功。备份为 `/var/lib/boya-agent/backups/boya_agent-20260908-100624-pre-e72589b.db`。

部署后只读数据库检查正常；邮件、摘要、Telegram、自动选课均关闭。队列只有 6079 条历史成功任务，没有待发送任务。登录接口空请求返回 `503 email_delivery_paused`，未提供收件人，也没有发送测试邮件。

## 性能观察与限制

部署执行期间从本地对公开 `/api/courses` 串行请求五次，均为 HTTP 200、2020 字节；总耗时依次为 449、107、441、101、85ms。该记录包含网络、TLS 和服务端时间，不是负载测试，也不能用于宣称本次提速。

依赖仍使用版本下限，尚未完整锁定传递依赖。隔离环境解决了默认解释器缺包的问题，不等于所有机器安装结果一致。真实课程、提醒设置完整流程、屏幕阅读器及长期负载仍未验证。

## 回退

本次无数据库迁移。必要时可回退本批前端和工作流改动，恢复 `0314a3b` 对应内容并通过正常部署流程发布；该版本保留邮件总开关。不要回退到不具备邮件总开关的更早版本，也不要为前端回退恢复数据库备份。

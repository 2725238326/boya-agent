# 2026-09-08 公开课程浏览

## 范围

基线为 `0c24161`。首页增加无需登录的课程列表；个人功能继续使用原有认证，不增加匿名提醒写入。全站邮件保持关闭。

- 课程状态、名额、报名时间优先展示；其他信息通过原生详情控件展开。
- 名称、校区和报名状态在已读取的最近最多 200 条有效记录中筛选，不重复请求接口；不承诺全库搜索。
- 公开课程接口新增 `source`，仅包含最近成功时间、采集中标记、异常标记，不返回内部错误内容。
- 门户区分页面加载时间与最近成功采集时间；无法确认时明确提示，不把接口生成时间当作采集时间。
- 统一邮件暂停说明，关闭订阅页发送按钮并阻止提交处理；已有验证码验证和个人会话不变。
- 修复“下一场开选”错误地只检查已经开放报名课程的问题。

## 验证

Python 3.12 隔离环境完整检查：74 项测试通过，5 项子测试通过；JavaScript 和 TypeScript 7 检查通过。新增测试验证公开元数据白名单、未来开选课程及邮件暂停页面。

门户八组回归与首页两个视口的场景检查通过。首页检查包括有课、未开选、已满、无课、筛选、请求失败恢复、文本转义、详情键盘展开与暂停提交。筛选过程中课程接口请求次数保持一次。390px 和 1280px 截图经过检查，并修复了手机筛选按钮竖排问题。

测试全部使用固定数据；没有连接真实校园账号或发送邮件。截图中的课程、邮箱和时间均为样例。

## 发布

应用提交 `990d404` 已部署到 `49.233.248.86`，访问入口为 `https://buaaboya.top`。[功能分支 CI](https://github.com/2725238326/boya-agent/actions/runs/34179930961) 和 [生产工作流](https://github.com/2725238326/boya-agent/actions/runs/34180049514) 均成功。

服务与 Nginx 为 active，健康接口成功。数据库备份为 `/var/lib/boya-agent/backups/boya_agent-20260908-102752-pre-990d404.db`，部署后只读完整性检查正常。邮件、摘要、Telegram、自动选课均为关闭；队列只有 6079 条历史成功任务，没有待发送任务。

线上 Chromium 在 390px 和 1280px 下无需登录即可读取两条现有课程记录，无脚本异常或横向溢出；订阅页发送按钮禁用，匿名个人会话接口返回 401。该检查只读取已有数据，不代表真实课程变化、报名结果或提醒投递已经验证。未发送测试邮件。

## 限制

未实现全库分页、真实课程效果测试和屏幕阅读器完整操作测试。原生前端与 Flask 保持不变。没有数据库迁移；回退本批提交不需要恢复数据库，但必须保留此前邮件总开关。
